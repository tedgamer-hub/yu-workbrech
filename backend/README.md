# backend

FastAPI service for Gaokao Workbench.

## Run local (without Docker)

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

## Current status

- endpoint skeleton created
- business logic pending
- persistence layer pending